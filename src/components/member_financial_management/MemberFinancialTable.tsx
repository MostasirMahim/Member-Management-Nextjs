import React from 'react';

const MemberFinancial = ({invoices}) => {
    console.log('invoices', invoices);
    return (
        <div>
            <h1>Member Financial Management</h1>
        </div>
    );
};

export default MemberFinancial;